<?xml version="1.0" encoding="utf-8"?>
<!-- android/app/src/main/res/values/styles.xml -->
<!-- So'zona — Launch theme qora background muammosini hal qiladi -->
<resources>
    <!-- Launch theme — ilova yuklanayotganda ko'rinadi -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Qora oyna o'rniga primary indigo color -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:statusBarColor">#4F46E5</item>
        <item name="android:navigationBarColor">#4F46E5</item>
    </style>

    <!-- Normal theme — Flutter engine ishga tushgandan keyin -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">#00000000</item>
    </style>
</resources>